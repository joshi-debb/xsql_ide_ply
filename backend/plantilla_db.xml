<database name="nombre_db">
    <tables>
        <table name="nombre_tabla_1">
            <fields>
                <field name="campo_1" type="tipo_dato_1" />
                <field name="campo_2" type="tipo_dato_2" />
                <!-- Otros campos -->
            </fields>
            <records>
                <record>
                    <field name="campo_1">valor_1</field>
                    <field name="campo_2">valor_2</field>
                    <!-- Otros valores de campos -->
                </record>
                <!-- Otros registros -->
            </records>
        </table>
        <!-- Otras tablas -->
    </tables>

    <functions>
        <function name="nombre_funcion_1">
            
            
        </function>
        <!-- Otras funciones -->
    </functions>
    
    <procedures>
        <procedure name="sp_nuevoprocedimiento">
            CREATE PROCEDURE sp_nuevoprocedimiento(@MONTO AS DECIMAL,@IDFACTURA INT)
            AS
            BEGIN
            DECLARE @IVA DECIMAL;
            DECLARE @ISR DECIMAL;
            SET @IVA = @MONTO - @MONTO/1.12;
            IF @MONTO > 2000 THEN
                SET @ISR = @MONTO *0.07;
            ELSE
            SET @ISR = @MONTO *0.10;
                END IF;
            UPDATE tbfactura set monto = @monto where idfactura = @idfactura;
            END;
        </procedure>
    </procedures>
</database>
