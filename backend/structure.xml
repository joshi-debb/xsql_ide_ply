<?xml version="1.0" encoding="utf-8"?>
<SCHEMAS>
	<current name="personas"/>
	<database name="personas">
		<tables/>
		<views/>
		<functions>
			<function name="insertaralumnos">CREATE FUNCTION insertaralumnos (@flag AS int) RETURNS int AS BEGIN IF @flag == 1 THEN INSERT INTO alumno (nombre, apellido, registro, grado, altura) VALUES ('juan', 'lopez', 1001, '1a', 1.80);INSERT INTO alumno (nombre, apellido, registro, grado, altura) VALUES ('ana', 'gomez', 1002, '2b', 1.55);INSERT INTO alumno (nombre, apellido, registro, grado, altura) VALUES ('carlos', 'perez', 1003, '3c', 1.65);INSERT INTO alumno (nombre, apellido, registro, grado, altura) VALUES ('maria', 'martinez', 1004, '4d', 1.77);INSERT INTO alumno (nombre, apellido, registro, grado, altura) VALUES ('pedro', 'rodriguez', 1005, '5e', 1.71);RETURN 1; ELSE THEN RETURN 0; END IF; END;</function>
		</functions>
		<procedures>
			<procedure name="creartablapersona">CREATE PROCEDURE creartablapersona () AS BEGIN CREATE TABLE persona ( nombre NVARCHAR(5) not null primary key, apellido NVARCHAR(5) not null, edad int not null, altura decimal not null, fecha date not null );END;</procedure>
			<procedure name="sp_conteo">CREATE PROCEDURE sp_conteo (@i int, @j AS decimal) AS BEGIN SELECT 'los valores recibidos en el procedimiento son: ';SELECT '@i = ' + @i + ' y @j = ' + @j;END;</procedure>
		</procedures>
	</database>
</SCHEMAS>
